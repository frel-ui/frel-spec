module blueprint.anonymous_blueprint_it

import test.common.*

blueprint TextWrapper(content: Blueprint<String>) {
    hw : String = "Hello!"
    content(hw)
}

blueprint App {
    TextWrapper { content ->
        text { "Message: ${content}" }
    }
}
