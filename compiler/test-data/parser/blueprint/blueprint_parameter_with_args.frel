module blueprint.blueprint_parameter_with_args

import builtin.text

blueprint TextWrapper(content: Blueprint<String>) {
    hw : String = "Hello World!"
    content(hw)
}

blueprint App {
    TextWrapper { s ->
        text { "The text is: ${s}" }
    }
}
