module blueprint.with_backend

import test.common.*

backend CounterBackend {
    count : i32 = 0

    command increment()
    command decrement()
}

blueprint CounterUI {
    with CounterBackend

    column {
        text { "${count}" }
        row {
            button { "+" } .. on_click { increment() }
            button { "-" } .. on_click { decrement() }
        }
    }
}
