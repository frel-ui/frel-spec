module instructions.decoration_tint

import test.common.*

blueprint DecorationTint {
    column {
        icon { "close" } .. tint { AppTheme.Black }
        icon { "star" } .. tint { #FFD700 }
        icon { "settings" } .. tint { rgba(100, 100, 100, 255) }
    }
}
