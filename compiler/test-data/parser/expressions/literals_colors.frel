module expressions.literals_colors

import test.common.*

blueprint ColorLiterals {
    column {
        box { } .. background { color: #FF0000 }
        box { } .. background { color: #FF0000FF }
        box { } .. background { color: rgb(255, 0, 0) }
        box { } .. background { color: rgba(255, 0, 0, 128) }
        box { } .. background { color: Red }
        box { } .. background { color: Blue }
        box { } .. background { color: Transparent }
    }
}
