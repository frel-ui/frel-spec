# Edge Case Parser Fixtures

- `empty_file.frel` is intentionally blank to verify the parser accepts an empty source file as a valid module-less input; it replaces the redundant `empty_theme.frel` case.
