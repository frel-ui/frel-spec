module control.when_with_else

import test.blueprint

blueprint WhenElse {
    isLoggedIn : bool = false

    when isLoggedIn {
        text { "Welcome back!" }
    } else {
        text { "Please log in" }
    }
}
