module control.errors.repeat_missing_on

import test.blueprint

backend ListBackend {
    items : List<String> = []
}

blueprint Bad {
    with ListBackend

    repeat items { item ->
        text { item }
    }
}
