module scheme.scheme_with_enum_field

enum Priority { Low Medium High Critical }

enum Status { Draft Pending Approved Rejected }

scheme Task {
    id : Uuid .. identity
    title : String
    priority : Priority
    status : Status
}
